const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const test=require('node:test');const root=path.resolve(__dirname,'..');const read=f=>fs.readFileSync(path.join(root,f),'utf8');
test('control center exposes implementation and support navigation',()=>{assert.match(read('src/features/super-admin/components/Sidebar.tsx'),/Implementation & support/);assert.match(read('src/features/super-admin/layouts/SuperAdminLayout.tsx'),/customer-operations/);assert.match(read('src/features/super-admin/components/Navbar.tsx'),/Coordinate tenant go-lives/)});
test('customer operations explains implementation health and support SLAs',()=>{const page=read('src/features/super-admin/components/CustomerOperationsPage.tsx');for(const label of ['Implementation & Support Operations','Tenant implementation portfolio','Support SLA worklist','Target go-live','Response SLA','Resolution SLA'])assert.match(page,new RegExp(label));assert.match(page,/CustomTable/);assert.match(page,/responsive: true/)});
test('forms use live tenant and owner selections with Yup validation',()=>{const page=read('src/features/super-admin/components/CustomerOperationsPage.tsx');assert.match(page,/super-admin\/tenants/);assert.match(page,/platform-customer-operations\/owners/);assert.match(page,/Formik/);assert.match(page,/Yup\.object/);assert.doesNotMatch(page,/Enter .* ID|MongoDB|ObjectId/i)});
