const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const test=require('node:test');const root=path.resolve(__dirname,'..');const read=f=>fs.readFileSync(path.join(root,f),'utf8');
test('AI high-impact use requires human review and risk assessment before approval',()=>{const service=read('server/services/ai-governance.service.ts');assert.match(service,/High-impact AI use cases require human review/);assert.match(service,/A risk assessment is required before approval/);assert.match(service,/High residual risk must be mitigated before approval/);assert.match(service,/Approved AI use cases require a review date/)});
test('critical AI incidents suspend use and response transitions require evidence',()=>{const service=read('server/services/ai-governance.service.ts');assert.match(service,/input\.severity === "critical"/);assert.match(service,/status: "suspended"/);assert.match(service,/AI incident cannot move from/);assert.match(service,/Resolution evidence is required/)});
test('AI governance uses dynamic compliance permissions instead of hardcoded role names',()=>{const routes=read('server/routes/ai-governance.routes.ts');assert.match(routes,/requirePermission\(Module\.COMPLIANCE, PermissionAction\.VIEW\)/);assert.match(routes,/PermissionAction\.CREATE/);assert.match(routes,/PermissionAction\.EDIT/);assert.match(routes,/PermissionAction\.APPROVE/);assert.doesNotMatch(routes,/requireRoles|SystemRole/)});
test('approval enforces lifecycle, independent review, future review dates and incident clearance',()=>{const service=read('server/services/ai-governance.service.ts');assert.match(service,/useCase\.status !== "under_review"/);assert.match(service,/Independent approval is required/);assert.match(service,/Review due date must be in the future/);assert.match(service,/Open critical incidents must be resolved before approval/)});
test('initial risk is server-classified from impact and sensitive data',()=>{const service=read('server/services/ai-governance.service.ts');const routes=read('server/routes/ai-governance.routes.ts');assert.match(service,/\["health", "biometric"\]/);assert.match(service,/\["student_records", "financial", "behavioral"\]/);assert.doesNotMatch(routes,/body\("riskLevel"\)/)});
