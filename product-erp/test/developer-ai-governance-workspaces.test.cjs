const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const test=require('node:test');const root=path.resolve(__dirname,'..');const read=f=>fs.readFileSync(path.join(root,f),'utf8');
test('responsible AI workspace covers registry assessment approval and incidents',()=>{const page=read('src/features/role-wise-features/ai-governance/components/AiGovernancePage.tsx');for(const label of ['Responsible AI Governance','Institutional AI use-case register','Versioned AI risk assessments','AI incident response','Human review required','Resolution evidence'])assert.match(page,new RegExp(label));assert.match(page,/type="users"/);assert.match(page,/CustomTable/)});
test('AI forms are validated without raw database identifiers',()=>{const page=read('src/features/role-wise-features/ai-governance/components/AiGovernancePage.tsx');assert.match(page,/Formik/);assert.match(page,/Yup\.object/);assert.doesNotMatch(page,/Enter .* ID|MongoDB|ObjectId/i)});
test('workspace actions follow dynamic compliance permissions',()=>{const page=read('src/features/role-wise-features/ai-governance/components/AiGovernancePage.tsx');assert.match(page,/useHasPermission\('compliance', 'create'\)/);assert.match(page,/useHasPermission\('compliance', 'edit'\)/);assert.match(page,/useHasPermission\('compliance', 'approve'\)/)});
test('overview uses live SVG analytics and exposes overdue reviews and record evidence',()=>{const page=read('src/features/role-wise-features/ai-governance/components/AiGovernancePage.tsx');const insights=read('src/features/role-wise-features/ai-governance/components/AiGovernanceInsights.tsx');assert.match(page,/detailPanel/);assert.match(insights,/<svg/);assert.match(insights,/Overdue reviews/);assert.match(insights,/statusDistribution/);assert.match(insights,/riskDistribution/)});
