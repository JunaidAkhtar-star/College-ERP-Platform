const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const test=require('node:test');const root=path.resolve(__dirname,'..');const read=f=>fs.readFileSync(path.join(root,f),'utf8');
test('advancement workspace connects funds campaigns pledges gifts and stewardship',()=>{const page=read('src/features/role-wise-features/advancement/components/AdvancementPage.tsx');for(const label of ['Advancement & Fundraising','Campaign portfolio','Pledge pipeline','Confirmed gift designations','Stewardship worklist'])assert.match(page,new RegExp(label));assert.match(page,/type="alumni"/);assert.match(page,/type="users"/);assert.match(page,/CustomTable/)});
test('continuing education explains catalog cohorts outcomes and credentials',()=>{const page=read('src/features/role-wise-features/continuing-education/components/ContinuingEducationPage.tsx');for(const label of ['Continuing Education','Program catalog','Delivery cohorts','Learner outcomes','Active credentials'])assert.match(page,new RegExp(label));assert.match(page,/Check|threshold|Finalize learner outcome/);assert.match(page,/CustomTable/)});
test('both workspaces use validated responsive forms without raw database IDs',()=>{const pages=read('src/features/role-wise-features/advancement/components/AdvancementPage.tsx')+read('src/features/role-wise-features/continuing-education/components/ContinuingEducationPage.tsx');assert.match(pages,/Formik/);assert.match(pages,/Yup\.object/);assert.match(pages,/responsive: true/);assert.doesNotMatch(pages,/Enter .* ID|MongoDB|ObjectId/i)});
